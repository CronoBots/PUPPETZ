# Dépendances Node
node_modules/

# Owners.json (cache des handles Twitter/X) est désormais COMMITTÉ comme seed :
# il évite au runner CI de re-scraper 748 profils à chaque run (rafale -> 429).

# PID de la session Remote Control (local à chaque PC)
.remote-control.pid

# Logs runtime du remote-control (local)
remote-control-loop.log
